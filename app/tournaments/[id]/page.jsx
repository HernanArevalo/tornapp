import tournaments from '../../../data/tournaments.json'
import styles from './tournament.module.css'
const tournament = tournaments[0]

export const metadata = {
  title: `Torneo - ${tournament.name.toUpperCase()}`,
  description: 'Generated by create next app'
}

export default function TournamentId ({ params }) {
  return (
    <>
      <div>
        <h2 className={ styles.h2 }>Tournament {tournament.name}</h2>

        <div className={ styles.grid }>
          <div className={ styles.gridHead}>
              <div className={ styles.position }>--</div>
              <div className={ styles.name }>EQUIPO</div>
              <div className={ styles.points }>PT</div>
              <div className={ styles.pg }>PG</div>
              <div className={ styles.pe }>PE</div>
              <div className={ styles.pp }>PP</div>
          </div>
          { tournament.teams.map((team, idx) =>(
            <div key={team.name} className={ styles.team}>
              <div className={ styles.position }>{idx}</div>
              <div className={ styles.name }>{team.name}</div>
              <div className={ styles.points }>{team.points}</div>
              <div className={ styles.pg }>{team.pg}</div>
              <div className={ styles.pe }>{team.pe}</div>
              <div className={ styles.pp }>{team.pp}</div>
            </div>
          ))
          }
        </div>

      </div>
    </>
  )
}
